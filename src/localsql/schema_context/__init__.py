"""Phase 5A schema-context analysis and deterministic budgeting.

Analysis/design only in this phase -- nothing here is wired into Phase 1-3
training/evaluation artifacts yet. No ML dependencies; pure Python +
`localsql.data.models` + `sqlglot` (already a base dependency).
"""
